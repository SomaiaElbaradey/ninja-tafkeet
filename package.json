{
  "name": "ninja-tafkeet",
  "version": "0.0.1",
  "description": "tafqeet from english numbers to arabic words",
  "private": false,
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "author": "SomaiaElbaradey",
  "license": "ISC",
  "scripts": {
    "dev": "vitest",
    "test": "vitest run",
    "build": "tsup index.ts --format cjs,esm --dts",
    "lint": "tsc",
    "release": "pnpm run lint && pnpm run test && pnpm run build && changeset publish --registry=https://npm.pkg.github.com/"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "prettier": "^3.1.1",
    "tsup": "^8.0.1",
    "typescript": "^5.3.3",
    "vitest": "^1.0.4"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/"
  }
}
